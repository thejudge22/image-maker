# Git specific files
.git
.gitignore
.gitattributes

# Node modules (rebuilt inside container)
node_modules
backend/node_modules

# Environment files (should be injected, not copied)
.env
backend/.env
backend/env.example # Example file is okay to copy if needed, but not the actual .env

# Docker specific files
Dockerfile*
docker-compose.yml
# Remove the line below to allow nginx.conf to be copied
# nginx.conf

# OS generated files
.DS_Store
Thumbs.db

# Log files
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Other potentially sensitive or large files
*.md
LICENSE
.vscode/