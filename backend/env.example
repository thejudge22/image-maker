# Gemini API Configuration
GEMINI_API_KEY=YOUR_GOOGLE_AI_API_KEY # Replace with your key

# OpenAI API Configuration
OPENAI_API_KEY=YOUR_OPENAI_API_KEY # Replace with your key

# Model Names (Adjust if necessary)
IMAGE_MODEL=imagen-3 # Or another available Imagen model (for Google)
REMIX_MODEL=gemini-1.5-flash # Or another available Gemini model (for Google)
# Note: OpenAI models will be selected in the frontend and sent in the request

# Server Configuration
PORT=3000
